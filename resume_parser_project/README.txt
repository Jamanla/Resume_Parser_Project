# Custom Resume Parser

## 📄 How to Use
1. Place `.pdf` or `.docx` resumes in the `resumes/` folder.
2. Run the parser:

```bash
python parser.py
```

3. Output will be saved to: `output/parsed_data.xlsx`

## 💡 Dependencies

Install required packages:

```bash
pip install pandas pdfplumber python-docx openpyxl
```
